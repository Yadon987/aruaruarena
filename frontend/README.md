# あるあるアリーナ - フロントエンド

React 18 + TypeScript + Vite で構築された「あるあるアリーナ」のフロントエンドアプリケーション。

## 技術スタック

- **React 18** - UIライブラリ
- **TypeScript 5** - 型安全な開発
- **Vite 7** - 高速なビルドツール（SWC使用）
- **SWC** - 超高速コンパイラ（Rust製）

## 開発環境のセットアップ

```bash
# 依存関係のインストール
npm install

# 開発サーバーの起動
npm run dev

# ブラウザで http://localhost:5173 にアクセス
```

## 利用可能なスクリプト

| コマンド | 説明 |
|---------|------|
| `npm run dev` | 開発サーバーを起動（HMR有効） |
| `npm run build` | 本番用ビルド |
| `npm run preview` | ビルド後のプレビュー |

## 環境変数

`.env` ファイルを作成して以下の環境変数を設定してください：

```env
VITE_API_URL=http://localhost:3000
```

詳しくは `.env.example` を参照してください。

## プロキシ設定

開発環境では `vite.config.ts` で設定されたプロキシを使用して、バックエンドAPI（`/api`）へ転送されます。

## バックエンドとの連携

- **開発環境**: Viteプロキシ経由で `http://localhost:3000` へ転送
- **本番環境**: 同じドメイン配下に配置されるためプロキシ不要
